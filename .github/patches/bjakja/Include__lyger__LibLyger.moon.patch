# Kainote ships a modified version of this file: this file is not published in the upstream repositories the build fetches, so the released copy is the only source.
# tier: carried
# Regenerate with tools in .github/patches/README.md; applied by .github/scripts/package.py.


--- a/Automation/automation/Include/lyger/LibLyger.moon
+++ b/Automation/automation/Include/lyger/LibLyger.moon
@@ -34,6 +34,8 @@
         @set_sub sub, sel, generate_furigana if sub
 
     set_sub: (@sub, @sel = {}, generate_furigana = false) =>
+        local has_script_info
+
         @script_info, @lines, @dialogue, @dlg_cnt = {}, {}, {}, 0
         for i, line in ipairs sub
             @lines[i] = line
